name: releases-on-darwin-aarch64

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build-on-darwin-aarch64.yml

  release:
    name: Release
    uses: ./.github/workflows/release.yml
    needs: build

name: releases-on-linux-armv7

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build-on-linux-armv7.yml

  release:
    name: Release
    uses: ./.github/workflows/release.yml
    needs: build

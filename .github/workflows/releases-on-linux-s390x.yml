name: releases-on-linux-s390x

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build-on-linux-s390x.yml

  release:
    name: Release
    uses: ./.github/workflows/release.yml
    needs: build

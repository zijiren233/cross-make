name: releases-on-linux-x86_64

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build-on-linux-x86_64.yml

  release:
    name: Release
    uses: ./.github/workflows/release.yml
    needs: build
